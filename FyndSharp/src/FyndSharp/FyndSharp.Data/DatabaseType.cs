using System;

namespace FyndSharp.Data
{
    /// <summary>
    /// 表示数据类型，如SQLServer，SQLite等
    /// </summary>
    public enum DatabaseType
    {
        SqlServer,
        Sqlite,
        MySQL
    }
}
